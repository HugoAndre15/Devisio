
{# templates/email/invoice.html.twig #}
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Facture {{ invoice.number }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #10b981;
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 8px 8px 0 0;
        }
        .content {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 0 0 8px 8px;
        }
        .invoice-info {
            background-color: white;
            padding: 15px;
            border-radius: 5px;
            margin: 20px 0;
        }
        .footer {
            text-align: center;
            font-size: 12px;
            color: #666;
            margin-top: 30px;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>{{ company.name }}</h1>
        <p>Nouvelle facture disponible</p>
    </div>

    <div class="content">
        <p>Bonjour {{ customer.firstName }} {{ customer.lastName }},</p>

        <p>Nous avons le plaisir de vous transmettre votre facture pour {{ invoice.subject }}.</p>

        <div class="invoice-info">
            <strong>Détails de la facture :</strong><br>
            <strong>Numéro :</strong> {{ invoice.number }}<br>
            <strong>Date :</strong> {{ invoice.invoiceDate|date('d/m/Y') }}<br>
            <strong>Échéance :</strong> {{ invoice.dueDate|date('d/m/Y') }}<br>
            <strong>Montant total :</strong> {{ invoice.formattedTotal }}
        </div>

        {% if invoice.description %}
            <p><strong>Description :</strong><br>
            {{ invoice.description|nl2br }}</p>
        {% endif %}

        {% if invoice.paymentTerms %}
            <p><strong>Modalités de paiement :</strong><br>
            {{ invoice.paymentTerms|nl2br }}</p>
        {% endif %}

        <p>Vous trouverez la facture complète en pièce jointe de cet email.</p>

        <p>Pour toute question, n'hésitez pas à nous contacter.</p>

        <p>Cordialement,<br>
        L'équipe {{ company.name }}</p>
    </div>

    <div class="footer">
        <p>{{ company.name }}<br>
        {{ company.address }}, {{ company.postalCode }} {{ company.city }}<br>
        {% if company.phone %}Tél : {{ company.phone }} - {% endif %}
        {% if company.email %}Email : {{ company.email }}{% endif %}</p>
    </div>
</body>
</html>